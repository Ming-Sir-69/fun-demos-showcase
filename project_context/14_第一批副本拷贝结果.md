# 第一批副本拷贝结果

## 1. 执行原则

本轮没有在原项目中做任何改动。

所有整理都发生在：

`selected_projects/`

内部的两个副本目录中。

## 2. Solution Diagram Workflow 副本

副本路径：

`selected_projects/01_solution_diagram_workflow/`

本轮已复制：

1. `Index.md`
2. `project_context/` 中的高密度决策、验收与交接文档
3. `transcripts/`
4. `guides/`
5. `specs/`
6. `templates/`
7. `src/`
8. `tests/`
9. `human-factors-diagram-renderer/`
10. `outputs/` 中的代表性 `sample` 与 `validation` 产物

本轮未复制：

1. 明确 `8d` 命名的历史资产与前置残留
2. 大量无关的实验目录
3. 非必要系统噪音文件

## 3. Excel Intelligent Translation 副本

副本路径：

`selected_projects/02_excel_intelligent_translation/`

本轮已复制：

1. `README.md`
2. `app.py`
3. `run.sh`
4. `build_mac.sh`
5. `ExcelIntelligentTranslator.spec`
6. `Excel智能翻译器_项目介绍.pptx`
7. `project_context/` 中的高密度架构与复盘文档
8. `src/`
9. 单一测试样本 `L9FHS003&04-DT-R 检验规范(SIP).xlsx`

本轮未复制：

1. 打包出的 macOS App 本体
2. 多余的测试 Excel
3. 环境缓存与构建噪音
4. 与主线无关的历史目录

## 4. 本轮额外处理

1. 已将 Excel 副本中的 API key 改为占位符
2. 已为两个副本补写第一版对外入口文件

## 5. 下一轮建议重点

1. 继续收紧两个副本的入口层结构
2. 再决定是否要继续删除 Solution 副本中仍保留但不必要的例子或引用
3. 再决定 Excel 副本里的测试样本命名是否需要泛化

## 6. 后续说明

上述内容只代表第一批拷贝状态。

第二轮开始，两个副本已经进入主动收缩阶段，后续收缩原则与结果见：

`project_context/15_第二轮副本收缩记录.md`
